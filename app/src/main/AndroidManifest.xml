<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.nan.myandroid">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_SETTINGS"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".network.NetworkActivity"></activity>
        <activity android:name=".keep_alive.ApplyRightActivity" />
        <activity android:name=".transition_animation.activity.AfterActivity" />
        <activity android:name=".transition_animation.TransitionActivity" />
        <activity android:name=".transition_animation.activity.PreActivity" />
        <activity android:name=".databinding.DataBindingActivity" />
        <activity android:name=".viewmodel.ViewModelActivity" />
        <activity
            android:name=".rengwuxian.view.CustomViewActivity"
            android:label="@string/title_activity_view"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".dialog_popupwindow.PopupWindowAndDialogActivity" />
        <activity android:name=".coroutines.CancelTimeoutActivity" />
        <activity android:name=".coroutines.KotlinActivity" />
        <activity android:name=".coroutines.BasicsActivity" />
        <activity android:name=".downloadmanager.DownloadManagerActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.nan.myandroid.FileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_path" />
        </provider>
    </application>

</manifest>